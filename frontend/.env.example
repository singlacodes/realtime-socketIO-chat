# Backend API URL used at build time (Vite)
# Local:
# VITE_SERVER_URL=http://localhost:8000
# Render (separate API service):
# VITE_SERVER_URL=https://your-backend.onrender.com
# Same-origin (API serves frontend): leave unset or empty
VITE_SERVER_URL=http://localhost:8000
